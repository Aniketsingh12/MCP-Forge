{% if kind == "python_sdk" %}# {{ api_title }} MCP server — no credentials required.
# Tools call the library in-process.
{% else %}# {{ api_title }} MCP server configuration
API_BASE_URL={{ base_url }}
# {{ credential_hint }}
API_CREDENTIAL=
API_TIMEOUT=30
{% endif %}