# {{ api_title }} — MCP Server

Generated by [MCP Forge](https://github.com/). An MCP server exposing
{{ tool_count }} tool{{ 's' if tool_count != 1 else '' }} for **{{ api_title }}**.

## Install

```bash
pip install -r requirements.txt
```

## Configure

Set the target API base URL and your credential:

```bash
export API_BASE_URL="{{ base_url }}"
export API_CREDENTIAL="<your-credential>"   # {{ credential_hint }}
```

Or copy `.env.example` to `.env` and fill it in.

## Run

```bash
python server.py            # stdio transport (for MCP clients)
```

## Use with Claude Desktop

Add this to your `claude_desktop_config.json`:

```json
{{ claude_config }}
```

## Tools

| Tool | Method | Description |
|------|--------|-------------|
{% for t in tools %}| `{{ t.name }}` | {{ t.method }} | {{ t.description }} |
{% endfor %}

## Test

```bash
pip install pytest
pytest
```

---
Generated from an OpenAPI specification. The auth, transport, and error-handling
code comes from audited templates; only the tool set reflects your API.
