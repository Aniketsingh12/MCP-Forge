"""
{{ api_title }} - MCP server.

Generated by MCP Forge from an OpenAPI specification.
Transport: stdio and streamable HTTP (via the official Python MCP SDK).

Configuration (environment variables):
  API_BASE_URL     Base URL for the target API (default baked in below).
  API_CREDENTIAL   {{ credential_hint }}
"""
from __future__ import annotations

import json
import os
from typing import Any, Optional
from urllib.parse import quote

import httpx
from mcp.server.fastmcp import FastMCP

# --------------------------------------------------------------------------- #
# Configuration
# --------------------------------------------------------------------------- #
BASE_URL = os.environ.get("API_BASE_URL", {{ base_url_literal }}).rstrip("/")
API_CREDENTIAL = os.environ.get("API_CREDENTIAL", "")
REQUEST_TIMEOUT = float(os.environ.get("API_TIMEOUT", "30"))

mcp = FastMCP({{ server_slug_literal }})
_client = httpx.Client(timeout=REQUEST_TIMEOUT)


# --------------------------------------------------------------------------- #
# Auth + request plumbing (from audited templates, not model-generated)
# --------------------------------------------------------------------------- #
def _auth(headers: dict[str, str], query: dict[str, Any]) -> Optional[tuple[str, str]]:
    """Inject credentials. Returns basic-auth tuple for httpx when applicable."""
{{ auth_block }}
    return None


def _request(
    method: str,
    path: str,
    path_params: dict[str, Any] | None = None,
    query: dict[str, Any] | None = None,
    body: Any = None,
) -> dict[str, Any]:
    """Perform an HTTP call and return a normalized result envelope.

    Success -> {"ok": True, "status": int, "data": ...}
    Failure -> {"ok": False, "status": int|None, "error": str, "data": ...}
    """
    url = BASE_URL + path
    for key, value in (path_params or {}).items():
        # Percent-encode: an unescaped value could otherwise inject extra path
        # segments or a query string into the URL.
        url = url.replace("{" + key + "}", quote(str(value), safe=""))

    headers: dict[str, str] = {"Accept": "application/json"}
    clean_query = {k: v for k, v in (query or {}).items() if v is not None}
    basic = _auth(headers, clean_query)

    try:
        response = _client.request(
            method,
            url,
            params=clean_query or None,
            json=body if body is not None else None,
            headers=headers,
            auth=basic,
        )
    except httpx.RequestError as exc:
        return {"ok": False, "status": None, "error": f"network error: {exc}", "data": None}

    return _normalize(response)


def _normalize(response: httpx.Response) -> dict[str, Any]:
    ctype = response.headers.get("content-type", "")
    if "application/json" in ctype:
        try:
            data: Any = response.json()
        except json.JSONDecodeError:
            data = response.text
    else:
        data = response.text

    if response.is_success:
        return {"ok": True, "status": response.status_code, "data": data}

    error = f"HTTP {response.status_code}"
    if isinstance(data, dict):
        for key in ("message", "error", "error_description", "detail"):
            if key in data and data[key]:
                error = f"{error}: {data[key]}"
                break
    return {"ok": False, "status": response.status_code, "error": error, "data": data}


# --------------------------------------------------------------------------- #
# Tools
# --------------------------------------------------------------------------- #
{{ tool_functions }}

if __name__ == "__main__":
    mcp.run()
