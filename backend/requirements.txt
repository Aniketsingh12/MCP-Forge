fastapi>=0.118,<1.0
uvicorn[standard]>=0.34
pydantic>=2.11,<3.0
jinja2>=3.1
httpx>=0.28
python-multipart>=0.0.20
pyyaml>=6.0
# Used two ways: the playground drives generated servers over the real MCP
# protocol, and generated servers import it at runtime.
# Pinned <2: mcp 2.0 replaced FastMCP with MCPServer, and generated servers
# target the 1.x FastMCP API that MCP client documentation still describes.
mcp>=1.2,<2
